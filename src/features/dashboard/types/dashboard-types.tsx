import { ElementType } from "react";

export type RoadmapCardProps = {
  title: string;
  description: string;
  href: string;
  level: string;
  chapters: string;
  projects: string;
  icon: ElementType;
};
